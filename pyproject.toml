[tool.pytest.ini_options]
pythonpath = ["src"]
asyncio_mode = "auto"
asyncio_default_fixture_loop_scope = "function"
markers = [
    "live: hits real external APIs, costs API quota — opt-in only, run with -m live",
]
addopts = "-m 'not live'"

[tool.ruff]
line-length = 100
target-version = "py312"
